<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste de courses</title>
</head>
<body>
    <h1>Créer une nouvelle liste de courses</h1>

    <form action="{{ path('shoppinglist_create') }}" method="POST">
        <input type="text" name="name" placeholder="Nom de la liste de courses" required>
        <button type="submit">Créer la liste</button>
    </form>

    <h2>Listes de courses connues</h2>
    <ul>
        {% for list in shoppingList %}
            <li>{{ list.name }}</li>
        {% else %}
            <li>Aucune liste de courses pour l'instant.</li>
        {% endfor %}
    </ul>
</body>
</html>
